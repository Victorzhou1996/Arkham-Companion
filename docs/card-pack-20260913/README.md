# 2026-09-13 卡图勘误更新

版本：`2026.09.13-cards`。基于已测试的 9 月 13 日增量更新版，不重新编译后端。

## 实际替换

| 分类 | 数量 | 处理 |
| --- | ---: | --- |
| 同名卡图内容变化 | 330 | 备份旧图后替换 |
| 新增主卡图与编号别名 | 771 | 补入，不删除本地独有文件 |
| 中文定制升级纸 | 44 | 放到中文资源路径，并加入语言索引 |
| 中文塔罗图 | 22 | 放到中文资源路径，并加入语言索引 |
| 密钥状态图 | 10 | 补入资源目录 |
| 完全相同的主卡图 | 7,507 | 不修改 |
| 可选“狗镇”头像 | 10 | 不覆盖标准调查员头像 |

总计替换或新增 1,177 个图片文件。原来的 `09081-t.avif` 以及两个历史来源记录 JSON 保留。
新包中的系统缓存、desktop.ini、说明文件、可选替换目录不进入运行时。

图包中的部分历史 Taboo 编号现在对应中文原始卡面，而非把所有禁卡表改动印在图上；实际规则和禁卡表说明仍由卡牌数据和后端决定。本次没有改卡牌规则或把所有这些图片声称为独立中文突变扫描。

## 记录和回退

- `audit.json`：原压缩包逐文件 SHA-256、尺寸、来源和初始目录对比。
- `replacement-plan.json`：最终采用的运行时路径、旧/新 SHA-256、替换或新增决策。
- `index.html`：左旧右新的原始图包对比页；最终是否采用以 replacement-plan 为准。
- 本地旧图备份：工作区 `backups/card-pack-20260913/`，含 `applied.json`。
- Kaho 旧静态文件备份：`/var/backups/arkham/card-pack-20260913-105413/`。
- 未更新 Online，未修改 Kaho 数据库、账号、存档或后端程序。

## 验证

- 新包可用图片 9,816 张均通过完整解码检查；不是只读文件扩展名。
- 前端 112 项测试通过，Build 13 项相关测试通过，两套前端类型检查和构建通过。
- 游戏和 Build 的卡图地址增加图包版本参数，避免旧缓存；原始图片名与卡牌编号不变。
- 新 DMG 全新安装和旧存档升级测试通过；两轮分别请求并校验全部 1,177 个新图地址。
- 游戏和管理工具入口、不同图标、两副预组、完整备份恢复和旧备份兼容性通过。
- Kaho 1,545 个差异静态文件已更新，1,177 个图片地址哈希校验通过，后端哈希不变。
- 图片语义做了代表性抽查及既有正反面编号规则核对，不声称人工逐字审阅了所有卡牌文本。

## 本地安装包

`Arkham-Horror-Local-App-macOS-arm64-20260913-cardfix.dmg`

SHA-256: `92238afeaaff6c774f0d986c9cd0dae5bfc967604837c7d2553de9b68a866c86`

安装前停止旧本地服务。游戏与管理工具一并拖入应用程序，玩家数据保留在用户数据目录。

## GitHub 和 Windows 接续

Mac 和 Server 分别提交到对应发布分支的 PR。由于此前 9 月 13 日的上游合并尚未上传，本次同时包含该版本的完整来源代码和已验证运行文件，而不只是几张图片。

- 共享主卡图：`shared/cards/`，必须执行 Git LFS 拉取。
- 游戏源代码：对应平台 `source/ArkhamHorror/`，包含中文资源与索引。
- Build 源码覆盖：`shared/source/arkham-build-overrides/`；保留嵌入模式与导入导出功能。
- Server 预编译 Linux amd64 后端 SHA-256：`70ddafe1dc30f412ff75c1d7dbda52aca99efa1dcf8fbfa9754f466a41bb5993`，与 Kaho 已验证版本一致。
- Windows 已使用 9 月 13 日同一后端的，只需更新前端、Build 和卡图库；更旧后端需从本次源代码构建，使用项目锁定的 GHC 9.14.1，不要只覆盖新版前端而保留旧 API。
- 更新已有数据库前备份，并使用项目的增量迁移流程；不要重建数据库、不要覆盖 JWT 或用户上传图片。
- 不包含玩家数据库、个人密钥或生成的客户端会话密钥。
