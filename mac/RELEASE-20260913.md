# Mac 2026.09.13-cards

See `docs/card-pack-20260913/README.md` and `replacement-plan.json` for the
complete changes and verification boundaries.

Artifact: `Arkham-Horror-Local-App-macOS-arm64-20260913-cardfix.dmg`.
SHA-256: `92238afeaaff6c774f0d986c9cd0dae5bfc967604837c7d2553de9b68a866c86`.

The game and manager have separate launchers and icons. Fresh installation,
old database migration, backup/restore and all 1,177 changed image URLs passed
against the actual mounted DMG. Stop the old local service before installation.

The published runtime is copied from the portable, signed DMG payload, not the
unrelocated compiler output. Player databases and generated session keys are
excluded. Full card images are also available in `shared/cards` via Git LFS.
