# 2026.09.23.1 Compatibility and Content

- Keep the chosen game UI until explicitly switched from Settings. Remove gameplay switches and ignore stale conflicting UI links.
- Fix classic UI decoding for BloodToken and ValueX locations. The supplied save, actual Chosen of Zburamoarte forced ability, and all eight Carcosa standalone openings pass regression checks.
- Move multiple ability-trigger settings into an independent menu so Empirical Hypothesis no longer shrinks the entire equipment row in compact layouts.
- Bundle five community starter decks, including complete notes, alongside the existing two. Existing players can add missing starter decks without replacing edited decks or notes.
- Add 52 authorized music files, mapped by scenario title rather than unreliable folder numbering. Music and voice have separate controls; autoplay, loop, volume and per-scenario manual selection persist locally.
- Apply 618 card-image changes: 299 replacements and 319 additions. Retain the existing readable, higher-resolution 10531b image after visual comparison; the supplied version is sideways with the same text. Optional alternate investigator portraits are not automatically applied.
- Include the previously missing zh-cn campaign translations, including The Circle Undone. Prevent untranslated English placeholders from overriding translated Chinese and share complete campaign messages with the classic UI.
- Refresh card-image caches in both game UIs and the bundled Build interface.

## Verification

- 1639 backend examples passed; 344 frontend tests and type checking passed.
- macOS and Linux isolated API tests cover fresh and restored databases, achievement settings, starter notes, idempotent starter additions, custom art, and bounded campaign setup transitions.
- Browser regression covers current/classic UI, desktop/mobile, persistent Settings switching, music playback/preferences, and an Empirical Hypothesis before/after size comparison.
- macOS release validation mounts the real DMG and uses isolated user data. It checks fresh installation, old-data preservation, backup/restore, all reviewed image files and all 52 audio files.
- These are targeted regressions, not a claim that every campaign has been played through in full.

Kaho is the deployment target for this release. The domestic Online service is intentionally unchanged.

## Building

Use the included source snapshot, not the older Server branch frontend. Run `git lfs pull` before building or packaging; audio and image assets require LFS contents. The frontend preparation step rebuilds the pinned classic compatibility bundle from `legacy-ui-v20260826.3/prepared`.
