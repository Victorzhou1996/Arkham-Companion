# Linux 独立服务器完整包 v20260924.1

[下载入口与校验](packages/README.md) · [部署指南](linux/self-hosted/README.md) · [升级/备份/回滚](linux/self-hosted/UPDATE.md)

此包与 Windows v20260923.2 去重完整版使用相同游戏内容，固定游戏提交为 `67b8f2a1aae9cf4e2b4c64f8895dd4fc20743e0c`。这是 Linux amd64 完整部署包，不是增量补丁，也不是 Windows 包改后缀。

## 内容及边界

- 前端 `2026.09.23.3-investigator-alignment`，当前/经典界面均包含，经典版本 `legacy-ui-20260923.1`。
- 同源自托管模式：`VITE_ONLINE_MODE=false`、本地账号布局接口，无 Companion 依赖。
- 完整后端、PostgreSQL 14、内部 nginx、Build、8,928 张主卡图、214 张头像、52 首音乐和 7 套预组卡组。
- 保留 Windows v20260923.2 的去重结果：419 个重复 Build 文件由旧 URL 兼容到新版；三套旧经典入口跳转到最新经典界面。
- Linux 非 root 启动、独立数据目录、SCRAM 密码、仅本机监听、systemd 安装入口、冷备和停服互斥保护保留。
- 八项数据库迁移使用执行记录保护，迁移前导出逻辑备份。现有数据升级请先自行停服、验证冷备并保留旧版本；不得覆盖原数据库或密钥。
- 不含账号、存档、密钥、测试数据或 Windows 管理工具。支持 Ubuntu 24.04 x86-64 / glibc 2.39+；不支持 ARM64/Alpine。

`server/release/`、游戏源码快照、共享卡图与既有 online 发布清单本次均未替换。新增独立包与更新启动层说明不代表已部署 Kaho/online，也没有发布 Windows/Mac 新包。

## 包校验

- 文件：`ArkhamHorror-Server-Linux-amd64-v20260924.1.tar.gz`
- 大小：1,723,822,865 bytes，约 1.61 GiB。
- SHA-256：`52a3593998b8cb836cae2f37af2ebd459ad3d11f07805cbbb289fccdd85873f7`
- 14,542 个归档文件逐文件哈希和执行权限验证；分发目录从未启动。
- 游戏/Build/卡图内容与 Windows v20260923.2 一致，只有 Linux 启动层和说明不同。

## 本次验证

隔离环境：本机 Ubuntu 24.04 / WSL2，应用和数据在 Linux 原生文件系统，专用非 root 测试用户；不使用玩家账号或线上存档。

- 新装启动 2.7 秒，重启 1.6 秒；脚本语法、依赖检查和 nginx 实际启动通过。
- 11 项状态/密钥/备份保护测试通过；SCRAM、错误密码拒绝、目录权限、本机监听、运行中备份拒绝、重复启动拒绝、systemd unit 语法通过。
- 注册、登录、7 套预组完整笔记、重复添加幂等、5 种撤回模式 × 成就开关、阶段提醒、自制卡上传/保存/发布通过。
- 14,180 个原资源 URL 返回正确哈希及相关 MIME，包含 419 个去重兼容 URL；52 首音乐 Range 请求、9 个旧经典入口跳转和不存在资源 404 通过。
- 浏览器实际使用斯特拉预组进入“聚集于此”，当前/经典界面可读取同一存档；旧经典书签保留游戏地址跳转，显示卡图无破图。Build 显示 7 套牌组及 1,818 张筛选后卡牌，所见图片无破图。
- WebSocket、重启后的 10 个原游戏 ID、7 个原牌组 ID、实际游戏资源/手牌、自制卡图和账号布局保留。
- 正常停服逻辑备份可读取；完整冷备 1,371 个文件逐一核对且权限私有。测试服务已停止。

未修改的前端沿用 Windows 发布时的 345 项测试、类型检查和构建结果，本次未重跑全部前端测试。没有宣称真实云服务器开机自启、公网 HTTPS、多人容量、完整战役通关或本次真实玩家旧存档迁移验收。

更新必须解压到新版本目录，不能直接覆盖旧目录，否则被精简的旧文件仍会留下。Git LFS 承载完整包；不要把百余字节的指针文件当作压缩包。
