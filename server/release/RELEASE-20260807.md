# Server Release 2026-08-07

This snapshot matches the build validated locally and deployed to the unkai
server on 2026-08-07. The online server was not modified by this release.

## Changes

- Every newly registered account receives two prebuilt decks copied from:
  - `https://arkham.build/deck/view/qewhl3Do4yFlzop`
  - `https://arkham.build/deck/view/DYKly6vcYWthFOi`
- The starter decks include their main deck, sideboard, taboo selection,
  customization choices, source link, and Chinese notes.
- Existing accounts are unchanged; the starter decks are created only during
  future registrations.
- Completed-campaign decks are available in the interlude upgrade selector.
- Final physical and mental trauma is written to the completed deck and restored
  when it is used to begin a later campaign.
- Build deck conversion preserves deck notes when a campaign branch is created.
- The server sidecar and its least-privilege database grant are included so
  deployments that route registration through the sidecar behave identically.

## Artifacts

- `server/release/bin/arkham-api`: Linux x86-64 backend.
- `server/release/frontend-dist/`: production game frontend.
- `server/release/build/`: production deck builder frontend.
- `server/source/ArkhamHorror/`: matching tracked source snapshot.
- `server/sidecar/`: registration, bug-report, and server helper API.
- `shared/cards/`: shared Chinese AVIF card library.

## Validation

- Backend library build completed all 6666 modules.
- Main frontend unit tests: 26 passed.
- Main frontend TypeScript/Vue check: passed.
- Build targeted tests: 5 passed.
- Build TypeScript/Svelte check: passed.
- Server sidecar tests: 18 passed.
- macOS and Linux release builds: passed.
- Local and unkai real-registration tests each created exactly two decks with
  the expected card counts, notes, sideboards, and customization metadata.
- Temporary test accounts were removed and the unkai database counts returned
  to their pre-deployment baseline.

## Integrity

- Linux backend SHA-256:
  `2acaa003e7b8760e3fc83542952030297873c769cd6d1c3f42921e62cf11d0ae`
- Game frontend `index.html` SHA-256:
  `14974415b78bd50407dd869122e4fabffd80a310e2d23b9841d13f4f8e1c1db4`
- Build frontend `index.html` SHA-256:
  `a63a313656d7856a9cbe8493f37d6984adf134858e76f402d2b492819899561f`
- Server update archive SHA-256:
  `eecf314b523606c8e26c8104cf81c68eb8710d02344292d0ee8c2c5f8b148594`

Live databases, accounts, saves, runtime logs, secrets, and deployment backups
are intentionally excluded.
