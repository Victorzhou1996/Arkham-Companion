# Server 2026.09.13-cards

See `docs/card-pack-20260913/README.md` and `replacement-plan.json` for the tested
card changes, source lineage, and Windows continuation notes.

The Linux amd64 backend is current for the September 13 integration, source
runtime stamp `21f0bd4fa2fac5180798c03b2e4b64dd47c0cc9f`; later source commits
change tests, packaging, frontend assets and cache revision, not backend rules.

Backend SHA-256: `70ddafe1dc30f412ff75c1d7dbda52aca99efa1dcf8fbfa9754f466a41bb5993`.

The native incremental schema is included as `server/release/native-setup.sql`.
Preserve existing database, keys and runtime settings. For older installations,
back up first and follow the source migration flow; never recreate player data.

For an outer Nginx frontend that serves built-in images directly, preserve the
native card-library priority. See the reference fragment in
`server/deploy/card-image-library-precedence.nginx.conf`, adjust the upstream
port/cache zone, and validate with `nginx -t` before applying it.

Kaho is updated. Online is intentionally unchanged.
